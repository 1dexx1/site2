---
import BlogCard from '@/components/BlogCard.astro'
import Container from '@/components/Container.astro'
import Link from '@/components/Link.astro'
import { buttonVariants } from '@/components/ui/button'
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from '@/components/ui/card'
import { SITE } from '@/consts'
import Layout from '@/layouts/Layout.astro'
import { getCollection } from 'astro:content'

const blog = (await getCollection('blog'))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, SITE.NUM_POSTS_ON_HOMEPAGE)
---
<div class="col-start-1 col-span-7 md:col-span-2 md:row-span-4 md:row-start-4 bg-[#171717]  p-5">
<Layout title="..." description={SITE.DESCRIPTION}>

  <Container class="flex flex-col gap-y-6">
    <h1>привет<h1/>
<br>
с самого начала, как только я начал разрабатывать этот блог, у меня было много вещей, которые нужно было тут сделать. Я сделал много чего, от кастомных страниц, до переписывания блога. И спустя сотни коммитов, я закрываю сайт. на этом сайте больне НЕ будут выходить обновления, если я захочу что-то сделать, мой гитхаб 1dexx1, все что я буду делать новое - СКОРЕЕ ВСЕГО будет там.
<br>
Спасибо! (исходный код сайта НЕ будет открыт)
  </Container>
</Layout>
